---
phase: 02-conversation-event-contract
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/hermes_agent_api_client/models.py
  - src/hermes_agent_api_client/__init__.py
  - tests/test_protocol.py
  - tests/test_package.py
autonomous: true
requirements:
  - TOOL-01
  - TERM-01
must_haves:
  truths:
    - "Consumers can import ToolProgressStatus and TerminalFailureReason from the package root."
    - "Direct ToolProgressEvent construction accepts only exact built-in 1-256 character visible-ASCII identifiers and the closed running/completed status enum."
    - "TerminalEvent remains immutable and exposes strict partial and failure_reason fields with safe defaults."
  artifacts:
    - path: "src/hermes_agent_api_client/models.py"
      provides: "Strict immutable Phase 2 public event vocabulary and shared lifecycle text validator"
      contains: "class ToolProgressStatus"
    - path: "src/hermes_agent_api_client/__init__.py"
      provides: "Exact package-root exports for both new public enums"
      contains: "TerminalFailureReason"
    - path: "tests/test_protocol.py"
      provides: "Direct-construction, exact-bound, strict-type, and immutability coverage"
    - path: "tests/test_package.py"
      provides: "Exact package-root and star-import contract coverage"
  key_links:
    - from: "src/hermes_agent_api_client/models.py"
      to: "src/hermes_agent_api_client/protocol.py"
      via: "private shared lifecycle-text validator reused by the wire DTOs in Plan 02-03"
      pattern: "_require_lifecycle_text"
    - from: "src/hermes_agent_api_client/__init__.py"
      to: "src/hermes_agent_api_client/models.py"
      via: "explicit imports and __all__ entries"
      pattern: "(ToolProgressStatus|TerminalFailureReason)"
---

<objective>
Define the immutable public conversation-event vocabulary and its exact direct-construction boundary.

Purpose: Satisfy TOOL-01 and TERM-01 while locking the public/private lifecycle-text parity required by D-05, D-06, D-07, and D-08 before wire decoding depends on it.
Output: Strict public enums and event fields, one shared input-value-free lifecycle text validator, exact package exports, and RED/GREEN/REFACTOR coverage.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
@/Users/amiller/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-conversation-event-contract/02-CONTEXT.md
@.planning/phases/02-conversation-event-contract/02-RESEARCH.md
@.planning/phases/02-conversation-event-contract/02-PATTERNS.md
@.planning/phases/02-conversation-event-contract/02-VALIDATION.md
@docs/superpowers/specs/2026-07-17-conversation-contract-design.md
@.planning/milestones/v0.1.0-phases/01-typed-hermes-api-client/01-SUMMARY.md

<interfaces>
Existing public base: `_FrozenModel` uses `ConfigDict(frozen=True, strict=True)`.
Existing enums: `TerminalOutcome` is a `StrEnum` with SUCCESS, LENGTH, and UPSTREAM_ERROR.
Existing event union: `HermesEvent` contains AssistantDeltaEvent, ToolProgressEvent, UsageEvent, KeepaliveEvent, and TerminalEvent.
Existing package facade: `src/hermes_agent_api_client/__init__.py` duplicates the public import list and exact `__all__` list deliberately.
</interfaces>
</context>

<feature>
  <name>Strict immutable public tool-progress and terminal vocabulary</name>
  <files>src/hermes_agent_api_client/models.py, src/hermes_agent_api_client/__init__.py, tests/test_protocol.py, tests/test_package.py</files>
  <read_first>
    - src/hermes_agent_api_client/models.py
    - src/hermes_agent_api_client/__init__.py
    - tests/test_protocol.py
    - tests/test_package.py
    - .planning/phases/02-conversation-event-contract/02-CONTEXT.md
    - .planning/phases/02-conversation-event-contract/02-PATTERNS.md
    - docs/superpowers/specs/2026-07-17-conversation-contract-design.md
  </read_first>
  <behavior>
    RED: Add tests that package-root imports expose `ToolProgressStatus.RUNNING`, `ToolProgressStatus.COMPLETED`, and `TerminalFailureReason.OUTPUT_TRUNCATED`, `AGENT_ERROR`, and `UNKNOWN`; the exact `__all__` and star-import assertions include both names.
    RED: Add direct-construction cases proving `tool_call_id` and `tool_name` accept exact built-in strings of lengths 1 and 256 containing only code points 0x21..0x7e, preserve punctuation and case exactly, and reject length 0/257, space/control/Unicode characters, `str` subclasses, bytes, and coercible values per D-05 through D-08.
    RED: Prove ToolProgressEvent requires enum instances rather than raw strings, contains only `tool_call_id`, `tool_name`, and `status`, and is frozen.
    RED: Prove TerminalEvent defaults to `partial=False` and `failure_reason=None`, requires an exact bool and exact enum-or-None, contains no raw code/text field, and remains frozen.
    GREEN: Add the closed StrEnum classes, update ToolProgressEvent and TerminalEvent, and expose both enums from the package root without broadening `HermesEvent` beyond the enriched existing variants.
    GREEN: Add one private 256-character visible-ASCII validator in models.py that first requires `type(value) is str`, never includes a rejected value in its exception, and is reusable by Plan 02-03 wire validation so D-08 cannot drift.
    REFACTOR: Keep enum placement, validator naming, and imports compact; retain the established frozen strict base and exact explicit export style.
  </behavior>
  <implementation>
    Follow the RED/GREEN/REFACTOR sequence and commit gates. In `models.py`, introduce `ToolProgressStatus`, `TerminalFailureReason`, `_LIFECYCLE_TEXT_MAX = 256`, and an input-value-free `_require_lifecycle_text(value: object) -> str` helper (or an equivalently named single helper), then use before-validation on both ToolProgressEvent identifier fields. The public status and failure fields remain strict enum-typed. In `__init__.py`, mirror both enums in the explicit imports and `__all__`. Do not add a runtime dependency, session headers, lifecycle tracking, raw error-code storage, or dependency upgrades; Ruff 0.15.22, prek 0.4.10, coverage 7.15.2, and GitPython 3.1.52 remain an explicit Phase 4 recheck/refresh constraint.
  </implementation>
  <acceptance_criteria>
    - TOOL-01 and TERM-01 tests fail before production changes and pass afterward.
    - All D-05 through D-08 exact type, range, preservation, and public/private parity prerequisites are encoded by one validator.
    - Existing direct model construction, union membership, frozen-model, and package export tests remain green.
    - No rejected lifecycle value appears in validator messages or exception representations.
  </acceptance_criteria>
  <verify>
    <automated>uv run --no-sync pytest tests/test_protocol.py tests/test_package.py --no-cov -q</automated>
  </verify>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python consumer to public model | Caller-controlled values enter immutable public event construction. |
| Public model to later wire decoder | One validator must enforce identical lifecycle text semantics at both construction paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | `ToolProgressEvent` direct construction | high | mitigate | Exact built-in type, 1-256 visible-ASCII validation, strict enums, and boundary cases in `tests/test_protocol.py`. |
| T-02-03 | Information Disclosure | lifecycle validator failures | medium | mitigate | Raise value-free validation failures and test public renderings contain no rejected input. |
| T-02-SC | Tampering | Python dependency set | low | accept | This plan installs nothing and changes no dependency metadata; recorded Ruff/prek/coverage/GitPython drift is rechecked and resolved in Phase 4. |
</threat_model>

## Artifacts this phase produces

New public symbols:
- `ToolProgressStatus`
- `TerminalFailureReason`

New private symbols:
- `_LIFECYCLE_TEXT_MAX`
- `_require_lifecycle_text` (or the executor's equivalently scoped single shared validator)

New fixture paths: none.

<verification>
- Run `uv run --no-sync pytest tests/test_protocol.py tests/test_package.py --no-cov -q` after GREEN and after REFACTOR.
- Run `uv run --no-sync pytest -q` at the end of Wave 1 so the 100% branch gate covers the new public paths.
- Confirm git history contains RED `test(02-01): ...` before GREEN `feat(02-01): ...`; add REFACTOR only if cleanup changes are made.
</verification>

<success_criteria>
- Consumers import the two closed enums and construct only the approved immutable event shapes.
- Direct public identifier validation exactly matches D-05 through D-08 and is ready for private reuse.
- Existing v0.1.0 public vocabulary and package-root behavior remain green.
</success_criteria>

<output>
Create `.planning/phases/02-conversation-event-contract/02-01-SUMMARY.md` when done.
</output>
