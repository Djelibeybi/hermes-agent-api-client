---
phase: 02-conversation-event-contract
plan: 04
type: tdd
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - src/hermes_agent_api_client/protocol.py
  - src/hermes_agent_api_client/sse.py
  - tests/test_sse.py
  - tests/test_transport.py
autonomous: true
requirements:
  - TERM-02
  - TERM-03
  - TERM-04
  - TERM-05
  - TERM-06
  - TERM-07
must_haves:
  truths:
    - "Normal stop, truncation, agent error, and unknown bounded safe code map only through the locked total terminal matrix."
    - "D-02: finish_reason=length accepts only completed absent/false, failed absent/false, partial absent/true, and error_code absent/output_truncated, and always exposes LENGTH, partial=true, and OUTPUT_TRUNCATED."
    - "D-03: finish_reason=error requires an exact server partial boolean, accepts completed absent/false and failed absent/true, maps absent/agent_error to UPSTREAM_ERROR with AGENT_ERROR, and maps another valid bounded safe code to UPSTREAM_ERROR with UNKNOWN."
    - "Explicit nulls, duplicate approved terminal keys, and every contradictory lifecycle combination fail closed without precedence guesses."
    - "Raw upstream error data never becomes public, and terminals remain invisible until suffix validation and cleanup succeed."
    - "Transport disconnects and cancellation retain their existing exception classifications and synthesize no terminal event."
  artifacts:
    - path: "src/hermes_agent_api_client/protocol.py"
      provides: "Omission-aware strict terminal metadata projection"
      contains: "error_code"
    - path: "src/hermes_agent_api_client/sse.py"
      provides: "Total terminal mapper integrated with the existing pending-terminal state machine"
      contains: "TerminalFailureReason"
    - path: "tests/test_sse.py"
      provides: "Exhaustive accepted/rejected matrix, duplicate, secrecy, cancellation, and suffix/cleanup coverage"
    - path: "tests/test_transport.py"
      provides: "Outer response-cleanup-before-terminal integration proof"
  key_links:
    - from: "src/hermes_agent_api_client/protocol.py"
      to: "src/hermes_agent_api_client/sse.py"
      via: "sanitized omission-aware lifecycle metadata passed to a total mapper"
      pattern: "(completed|failed|partial|error_code)"
    - from: "src/hermes_agent_api_client/sse.py"
      to: "_SSEDecoder._pending_terminal"
      via: "enriched TerminalEvent stored, not yielded immediately"
      pattern: "_pending_terminal"
    - from: "src/hermes_agent_api_client/sse.py"
      to: "tests/test_transport.py"
      via: "source and HTTP response cleanup complete before outer terminal yield"
      pattern: "terminal_is_delivered_only_after_response_cleanup"
---

<objective>
Implement the total safe terminal metadata contract without weakening the existing two-stage terminal commit.

Purpose: Deliver TERM-02 through TERM-07 exactly as D-01 through D-04 and D-09 through D-12 specify, using the D-13/D-14 evidence corpus and honoring the D-15/D-16 version gate already completed by Plan 02-02.
Output: Omission-aware terminal metadata validation, exhaustive mapping/rejection logic, enriched delayed terminal values, and RED/GREEN/REFACTOR lifecycle/security coverage.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
@/Users/amiller/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-conversation-event-contract/02-CONTEXT.md
@.planning/phases/02-conversation-event-contract/02-RESEARCH.md
@.planning/phases/02-conversation-event-contract/02-PATTERNS.md
@.planning/phases/02-conversation-event-contract/02-VALIDATION.md
@docs/superpowers/specs/2026-07-17-conversation-contract-design.md
@.planning/phases/02-conversation-event-contract/02-02-SUMMARY.md
@.planning/phases/02-conversation-event-contract/02-03-SUMMARY.md

<interfaces>
Plan 02-01 provides strict `TerminalEvent(outcome, partial=False, failure_reason=None)` and `TerminalFailureReason`.
Plan 02-02 provides immutable length, agent-error, tagged-contradiction, and design-matrix fixtures plus an implementation-time D-15/D-16 result.
Plan 02-03 provides `_JsonObjectPairs`, path-aware projection, duplicate-aware root `hermes` and choice `finish_reason`, and input-independent parser failure reduction.
Existing `_SSEDecoder._pending_terminal`, `finalize()`, `async_decode_hermes_sse`, and the outer client response scope implement delayed terminal delivery and cleanup precedence.
</interfaces>
</context>

<feature>
  <name>Total omission-aware safe terminal mapping and delayed delivery</name>
  <files>src/hermes_agent_api_client/protocol.py, src/hermes_agent_api_client/sse.py, tests/test_sse.py, tests/test_transport.py</files>
  <read_first>
    - src/hermes_agent_api_client/protocol.py
    - src/hermes_agent_api_client/sse.py
    - tests/test_sse.py
    - tests/test_transport.py
    - src/hermes_agent_api_client/models.py
    - tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_length.sse
    - tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_agent_error.sse
    - tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_task_exception_contradiction.sse
    - tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_design_matrix.json
    - tests/fixtures/hermes/v2026.7.7.2/provenance.json
    - .planning/phases/02-conversation-event-contract/02-CONTEXT.md
    - .planning/phases/02-conversation-event-contract/02-PATTERNS.md
  </read_first>
  <behavior>
    RED: Execute every D-01 stop combination: completed missing/true, failed missing/false, partial missing/false, error_code missing map to SUCCESS/false/None; any other present value or combination fails.
    RED: Execute every D-02 length combination: completed missing/false, failed missing/false, partial missing/true, error_code missing/output_truncated map to LENGTH/true/OUTPUT_TRUNCATED; any other combination fails.
    RED: Execute every D-03 error combination: root `hermes` and an exact present partial bool are required; completed missing/false and failed missing/true are accepted; an absent or `agent_error` code maps to the exact triple `TerminalOutcome.UPSTREAM_ERROR`, server partial, `TerminalFailureReason.AGENT_ERROR`, while another exact 1-256 visible-ASCII code maps to `TerminalOutcome.UPSTREAM_ERROR`, server partial, `TerminalFailureReason.UNKNOWN`.
    RED: For every approved metadata field, explicit null, integer/string boolean lookalikes, wrong types, over-bound codes, and same/conflicting duplicate members fail as HermesProtocolError per D-04/D-12. The tagged completed=true plus failed=true task-exception fixture must fail.
    RED: Root `hermes` may be absent for stop/length but not error per D-10. Same-named lifecycle fields outside root `hermes` and unknown fields inside it are ignored per D-09/D-11; duplicates confined to ignored additive/raw error objects remain ignored.
    RED: Pin choice `finish_reason` presence separately from lifecycle metadata: an absent `finish_reason` field is a validation failure, while an explicitly present JSON null is a valid nonterminal chunk and emits no terminal event by itself. A later valid DONE may still establish the existing default success terminal.
    RED: Raw error messages, objects, exception type names, and unknown raw code values are absent from public events, error renderings, cause/context, package traceback locals, and retained decoder/generator state. Unknown safe codes expose only UNKNOWN.
    RED: A valid standalone DONE retains the existing default success terminal, while any finish-derived terminal remains pending through DONE, suffix/EOF validation, source close, and HTTP response cleanup. Post-terminal data, duplicate DONE, cleanup failure, and malformed suffix expose no terminal.
    RED: Disconnects remain HermesTransportError where currently classified, cancellation identity remains CancelledError after cleanup, and neither path synthesizes a terminal.
    GREEN: Add omission-aware terminal metadata representation that cannot conflate missing with null, validates exact booleans and bounded safe codes, and accepts lifecycle fields only from root `hermes`.
    GREEN: Replace finish-reason precedence with a total mapper whose only accepted rows are D-01 through D-03 and whose failure result is input-independent. Construct TerminalFailureReason explicitly and discard raw error/code details.
    GREEN: Enrich only the TerminalEvent value stored by `_SSEDecoder._pending_terminal`; do not move finalization or the outer response cleanup gate.
    REFACTOR: Express the matrix with named private constants/helpers rather than nested precedence, keep every failure frame raw-input-free, and retain existing protocol-over-cleanup/cancellation precedence.
  </behavior>
  <implementation>
    Follow RED/GREEN/REFACTOR commits. In `protocol.py`, extend the Plan 02-03 projector for approved root `hermes` members `completed`, `failed`, `partial`, and `error_code`, rejecting duplicates before ordinary mapping construction. Represent omission with a private sentinel or presence-bearing frozen DTO that wire input cannot supply; validate booleans by identity and safe codes through the shared exact lifecycle-text validator. Preserve the existing distinction that choice-level `finish_reason` is required but may explicitly be null. In `sse.py`, map only the locked rows to complete public TerminalEvent values and return a closed input-independent failure sentinel for every other row. Do not search other object paths, publish the unknown raw code, retain raw `error` fields, or resolve contradictions by field precedence. Preserve `_pending_terminal`, `finalize()`, source `aclose()`, outer response scope, transport error, and cancellation paths. Read Plan 02-02's `release_verification`, structured `difference_summary`, and version-specific fixtures: if it reports a newer tag with machine-verified identical public semantics, implement only that exact version-specific projection and prove its public events equal canonical events. If release verification is `latest-tag-verification-blocked`, or the newer shape is ambiguous/incompatible, stop before code and surface the blocking human `checkpoint:decision` required by D-16; never invent an alias. Make no Phase 3 session-header, consumer tracker, Phase 4 dependency refresh, or distribution change. The recorded Ruff/prek/coverage/GitPython drift remains visibly deferred to Phase 4.
  </implementation>
  <acceptance_criteria>
    - TERM-02 through TERM-07 tests fail before implementation and pass after it.
    - Every accepted matrix row produces the exact outcome/partial/failure_reason triple and every unlisted row fails closed.
    - Error rows explicitly produce `TerminalOutcome.UPSTREAM_ERROR`; absent `finish_reason` fails while explicit null produces no terminal by itself.
    - D-09 through D-12 path, omission/null, additive-data, and duplicate semantics are exercised from raw JSON bytes.
    - No terminal is observable before all decoder/source/response gates succeed, and no exception path synthesizes one.
    - Raw terminal canaries and unknown raw codes never survive the protocol boundary.
    - A newer tag is implemented only from the declared version-specific evidence and structured comparison; blocked, incompatible, or ambiguous evidence stops at a human contract decision.
  </acceptance_criteria>
  <verify>
    <automated>uv run --no-sync pytest tests/test_sse.py tests/test_transport.py --no-cov -q</automated>
  </verify>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted chat chunk to terminal metadata DTO | Presence, duplicate, exact-type, bound, and cross-field consistency are established. |
| Safe terminal DTO to public TerminalEvent | Only closed outcome/partial/failure facts may cross; raw error material stays private. |
| Pending terminal to consumer | Suffix validation and source/HTTP cleanup must commit before observation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | terminal pair projection and exact field validation | high | mitigate | Preserve approved pairs, distinguish missing/null, validate exact booleans/codes, and test raw same/conflicting duplicates. |
| T-02-03 | Information Disclosure | root `hermes` raw error data and failure frames | high | mitigate | Project only lifecycle facts, map unknown codes to UNKNOWN, scrub raw state, and inspect traceback/generator canaries. |
| T-02-04 | Tampering / Spoofing | total terminal mapper | high | mitigate | Encode only D-01 through D-03; reject tagged and generated contradictions rather than applying precedence. |
| T-02-05 | Tampering | `_pending_terminal`, source close, and response cleanup | high | mitigate | Preserve the two-stage terminal commit and test suffix, duplicate DONE, cleanup failure, and cancellation ordering. |
| T-02-DOS | Denial of Service | `error_code` | medium | mitigate | Enforce the shared 256-character exact visible-ASCII bound before mapping. |
| T-02-SC | Tampering | package dependencies | low | accept | No dependency change; Phase 4 revalidates and refreshes the recorded Ruff/prek/coverage/GitPython drift. |
</threat_model>

## Artifacts this phase produces

New public symbols: none beyond Plan 02-01.

New private symbols:
- an omission sentinel or equivalently unforgeable presence representation for approved terminal fields
- a frozen omission-aware terminal metadata DTO
- a total terminal mapping helper and input-independent mapping-failure sentinel
- exact accepted-row constants/helpers for stop, length, and error

New fixture paths: none beyond Plan 02-02.

<verification>
- Run `uv run --no-sync pytest tests/test_sse.py tests/test_transport.py --no-cov -q` after RED, GREEN, and REFACTOR.
- Run `uv run --no-sync pytest -q`, `uv run --no-sync ruff check .`, and `uv run --no-sync basedpyright` after Wave 3; all complete under the Nyquist phase gate without refreshing dependencies.
- Confirm RED `test(02-04): ...` precedes GREEN `feat(02-04): ...`; add REFACTOR only if behavior-preserving cleanup occurs.
</verification>

<success_criteria>
- Consumers receive exact safe terminal facts for every approved row and HermesProtocolError for every ambiguous row.
- Raw terminal errors never cross into values, exceptions, frames, or retained state.
- Existing terminal withholding, transport, cancellation, cleanup, framing, and v0.1.0 regression guarantees remain green.
</success_criteria>

<output>
Create `.planning/phases/02-conversation-event-contract/02-04-SUMMARY.md` when done.
</output>
