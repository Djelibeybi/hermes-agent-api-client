---
phase: 02-conversation-event-contract
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/hermes/v2026.7.7.2/chat_completions/tool_progress_pair.sse
  - tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_length.sse
  - tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_agent_error.sse
  - tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_task_exception_contradiction.sse
  - tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_design_matrix.json
  - tests/fixtures/hermes/v2026.7.7.2/provenance.json
autonomous: true
requirements:
  - TOOL-02
  - TOOL-03
  - TOOL-04
  - TERM-02
  - TERM-03
  - TERM-04
  - TERM-05
  - TERM-06
must_haves:
  truths:
    - "Every accepted or rejected lifecycle fixture has immutable source identity, an honest evidence kind, semantic assertions, and a matching SHA-256."
    - "Canonical v2026.7.7.2 and the latest numeric Hermes tag are verified before implementation proceeds."
    - "Tag-source-derived combinations and design-derived contract rows are never represented as live captures."
  artifacts:
    - path: "tests/fixtures/hermes/v2026.7.7.2/chat_completions/tool_progress_pair.sse"
      provides: "Correlated running/completed lifecycle evidence"
    - path: "tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_design_matrix.json"
      provides: "Approved design-derived omission and unknown-code rows"
    - path: "tests/fixtures/hermes/v2026.7.7.2/provenance.json"
      provides: "Evidence kind, release, commit, source, semantic assertions, reproduction, and hashes"
  key_links:
    - from: "tests/fixtures/hermes/v2026.7.7.2/provenance.json"
      to: "tests/fixtures/hermes/v2026.7.7.2/chat_completions/"
      via: "one manifest entry and exact SHA-256 per immutable fixture"
      pattern: "(tag-source-derived|design-derived)"
    - from: "tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_design_matrix.json"
      to: ".planning/phases/02-conversation-event-contract/02-CONTEXT.md"
      via: "case-level decision citations for D-01 through D-04"
      pattern: "D-0[1-4]"
---

<objective>
Freeze the versioned evidence corpus before production mapping code is written.

Purpose: Make D-01 through D-16 auditable at the exact upstream/version boundary, including truthful derivation under D-13 and D-14, the implementation-time latest-tag gate in D-15, and the block-or-version-specific rule in D-16.
Output: One correlated tool fixture, three tag-source-derived terminal fixtures, one design-derived terminal matrix, and complete immutable provenance.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-conversation-event-contract/02-CONTEXT.md
@.planning/phases/02-conversation-event-contract/02-RESEARCH.md
@.planning/phases/02-conversation-event-contract/02-PATTERNS.md
@.planning/phases/02-conversation-event-contract/02-VALIDATION.md
@docs/superpowers/specs/2026-07-17-conversation-contract-design.md
@tests/fixtures/hermes/v2026.7.7.2/chat_completions/complete.sse
@tests/fixtures/hermes/v2026.7.7.2/provenance.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the immutable release target and freeze correlated tool evidence</name>
  <files>tests/fixtures/hermes/v2026.7.7.2/chat_completions/tool_progress_pair.sse, tests/fixtures/hermes/v2026.7.7.2/provenance.json</files>
  <read_first>
    - tests/fixtures/hermes/v2026.7.7.2/chat_completions/tool_progress_pair.sse (create; verify absent before writing)
    - tests/fixtures/hermes/v2026.7.7.2/provenance.json
    - tests/fixtures/hermes/v2026.7.7.2/chat_completions/complete.sse
    - .planning/phases/02-conversation-event-contract/02-CONTEXT.md
    - .planning/phases/02-conversation-event-contract/02-RESEARCH.md
    - .planning/phases/02-conversation-event-contract/02-PATTERNS.md
  </read_first>
  <action>Before changing fixture bytes, enumerate official Hermes numeric release tags and independently verify that canonical annotated tag `v2026.7.7.2` still peels to `9de9c25f620ff7f1ce0fd5457d596052d5159596`. Record the dynamically observed latest numeric tag and its peeled commit in a `release_verification` object in provenance. If it is canonical, record `compatibility_disposition: canonical-current`. If a newer tag exists, inspect its exact tagged handler/test shape and apply D-15/D-16: continue only after storing immutable version-specific captured evidence under that tag's own fixture root, SHA-256 linking every evidence path, and recording `compatibility_disposition: identical-public-semantics`, the latest release/commit, commit-pinned source references, `evidence_kind: immutable-tag-capture`, `public_semantics: identical`, a non-empty `reproduction.mode` and `reproduction.procedure`, `live_server_invoked: false`, and non-empty semantic assertions in `release_verification.latest_evidence`; otherwise stop as a blocking contract decision and add no generic alias. Record the same disposition and evidence paths in the plan summary for Plan 02-04. Then create a complete immutable SSE sequence containing tag-shaped `running` followed by `completed` for the same `toolCallId`, including the tag's additive emoji/label only where its source emits them, followed by a valid stop and DONE. Add a `tag-source-derived` manifest entry with exact canonical release and peeled commit, commit-pinned `source_refs`, a non-empty `reproduction.mode` and `reproduction.procedure`, `reproduction.configuration.live_server_invoked: false`, ordered-correlation `semantic_assertions`, and the computed SHA-256. Do not call this fixture captured, do not include invented arguments/results, and do not change dependencies.</action>
  <acceptance_criteria>
    - The canonical tag independently peels to the pinned commit, and provenance records the dynamically observed latest numeric tag plus peeled commit.
    - A canonical-current latest tag is accepted directly; a newer tag is accepted only with immutable version-specific captured evidence and an `identical-public-semantics` disposition, while incompatible or ambiguous shapes stop execution under D-16.
    - The fixture preserves the exact correlated ID and running/completed order required by TOOL-02.
    - The manifest truthfully classifies derivation and machine-verifies canonical release/source identities, reproduction metadata, `live_server_invoked: false`, semantic assertions, and matching SHA-256 bytes.
  </acceptance_criteria>
  <verify>
    <automated>uv run --no-sync python -c 'import hashlib,json,pathlib,subprocess; repo="https://github.com/NousResearch/hermes-agent.git"; source_repo="https://github.com/NousResearch/hermes-agent"; canonical="v2026.7.7.2"; canonical_commit="9de9c25f620ff7f1ce0fd5457d596052d5159596"; rows=[line.split() for line in subprocess.check_output(["git","ls-remote","--tags",repo,"v[0-9]*"],text=True).splitlines()]; refs={ref.removeprefix("refs/tags/"):sha for sha,ref in rows if not ref.endswith("^{}")}; peels={ref.removeprefix("refs/tags/").removesuffix("^{}"):sha for sha,ref in rows if ref.endswith("^{}")}; tags=[tag for tag in refs if all(part.isdigit() for part in tag.removeprefix("v").split("."))]; latest=max(tags,key=lambda tag:tuple(int(part) for part in tag[1:].split("."))); latest_commit=peels.get(latest,refs[latest]); assert canonical in refs and canonical in peels and peels[canonical]==canonical_commit; root=pathlib.Path("tests/fixtures/hermes/v2026.7.7.2"); data=json.loads((root/"provenance.json").read_text()); assert data["hermes_release"]==canonical and data["source_commit"]==canonical_commit and data["source_repository"]==source_repo; release=data["release_verification"]; assert release["canonical_tag"]==canonical and release["canonical_peeled_commit"]==canonical_commit and release["observed_latest_numeric_tag"]==latest and release["observed_latest_peeled_commit"]==latest_commit; latest_evidence=release.get("latest_evidence",{}); latest_paths=latest_evidence.get("fixture_paths",[]); latest_hashes=latest_evidence.get("fixture_sha256",{}); latest_reproduction=latest_evidence.get("reproduction",{}); newer_ok=release.get("compatibility_disposition")=="identical-public-semantics" and latest_evidence.get("hermes_release")==latest and latest_evidence.get("source_commit")==latest_commit and latest_evidence.get("evidence_kind")=="immutable-tag-capture" and latest_evidence.get("public_semantics")=="identical" and latest_evidence.get("live_server_invoked") is False and isinstance(latest_evidence.get("source_refs"),list) and len(latest_evidence["source_refs"])&gt;0 and all(ref.startswith(source_repo+"/blob/") and latest_commit in ref for ref in latest_evidence["source_refs"]) and isinstance(latest_reproduction.get("mode"),str) and bool(latest_reproduction["mode"]) and isinstance(latest_reproduction.get("procedure"),list) and len(latest_reproduction["procedure"])&gt;0 and isinstance(latest_evidence.get("semantic_assertions"),list) and len(latest_evidence["semantic_assertions"])&gt;0 and isinstance(latest_paths,list) and len(latest_paths)&gt;0 and set(latest_paths)==set(latest_hashes) and all(pathlib.Path(path).is_file() and path.startswith("tests/fixtures/hermes/"+latest+"/") and hashlib.sha256(pathlib.Path(path).read_bytes()).hexdigest()==latest_hashes[path] for path in latest_paths); assert (latest==canonical and release.get("compatibility_disposition")=="canonical-current") or (latest!=canonical and newer_ok); entries={entry["path"]:entry for entry in data["fixtures"]}; path="chat_completions/tool_progress_pair.sse"; entry=entries[path]; fixture=root/path; assert entry["evidence_kind"]=="tag-source-derived" and entry["hermes_release"]==canonical and entry["source_commit"]==canonical_commit and isinstance(entry.get("source_refs"),list) and len(entry["source_refs"])&gt;0 and all(ref.startswith(source_repo+"/blob/") and canonical_commit in ref for ref in entry["source_refs"]) and isinstance(entry.get("reproduction"),dict) and isinstance(entry["reproduction"].get("mode"),str) and bool(entry["reproduction"]["mode"]) and isinstance(entry["reproduction"].get("procedure"),list) and len(entry["reproduction"]["procedure"])&gt;0 and entry["reproduction"].get("configuration",{}).get("live_server_invoked") is False and isinstance(entry.get("semantic_assertions"),list) and len(entry["semantic_assertions"])&gt;0 and fixture.is_file() and hashlib.sha256(fixture.read_bytes()).hexdigest()==entry["sha256"]'</automated>
  </verify>
  <done>Canonical and latest release identity is proven, and the immutable correlated tool fixture is hash-linked to honest tagged-source provenance.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze terminal acceptance, unknown-code, and contradiction evidence</name>
  <files>tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_length.sse, tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_agent_error.sse, tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_task_exception_contradiction.sse, tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_design_matrix.json, tests/fixtures/hermes/v2026.7.7.2/provenance.json</files>
  <read_first>
    - tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_length.sse (create; verify absent before writing)
    - tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_agent_error.sse (create; verify absent before writing)
    - tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_task_exception_contradiction.sse (create; verify absent before writing)
    - tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_design_matrix.json (create; verify absent before writing)
    - tests/fixtures/hermes/v2026.7.7.2/provenance.json
    - .planning/phases/02-conversation-event-contract/02-CONTEXT.md
    - .planning/phases/02-conversation-event-contract/02-RESEARCH.md
    - docs/superpowers/specs/2026-07-17-conversation-contract-design.md
  </read_first>
  <action>Create complete tag-source-derived SSE fixtures for the canonical length row, the canonical valid agent-error row with exact server partial, and the tagged task-exception contradiction with `completed=true` plus `failed=true`. Create `terminal_design_matrix.json` for the explicitly approved but non-emitted omission cases and bounded unknown-code case; each row must state expected accept/reject semantics and cite D-01, D-02, D-03, or D-04. Keep lifecycle metadata only in root `hermes` per D-09/D-10, include ignored raw error canaries only to prove D-11, and represent duplicate-member cases later as raw bytes because ordinary JSON objects cannot preserve them under D-12. Add one complete provenance entry per file using the explicit path-to-kind mapping enforced below: the three SSE paths are `tag-source-derived`, while the matrix is `design-derived`. Every tag-source-derived entry must carry exact canonical release/commit plus non-empty commit-pinned `source_refs`; the design-derived entry must carry exact `decision_refs` D-01 through D-04. Every entry must also carry a non-empty `reproduction.mode` and `reproduction.procedure`, `reproduction.configuration.live_server_invoked: false`, non-empty `semantic_assertions`, and its exact computed SHA-256 per D-13/D-14. Do not label source-derived or design-derived bytes as captured. Record in the plan summary that Ruff 0.15.22, prek 0.4.10, coverage 7.15.2, and GitPython 3.1.52 are active Phase 4 currency constraints and make no Phase 2 pin/lock edit.</action>
  <acceptance_criteria>
    - Valid length and agent-error rows, the tagged contradiction, approved omissions, and the unknown bounded code each have immutable evidence.
    - D-01 through D-04 and D-09 through D-14 are traceable from fixture rows and provenance without overstating origin.
    - Every new fixture hash matches and no dependency file changes.
  </acceptance_criteria>
  <verify>
    <automated>uv run --no-sync python -c 'import hashlib,json,pathlib; root=pathlib.Path("tests/fixtures/hermes/v2026.7.7.2"); canonical="v2026.7.7.2"; commit="9de9c25f620ff7f1ce0fd5457d596052d5159596"; source_repo="https://github.com/NousResearch/hermes-agent"; data=json.loads((root/"provenance.json").read_text()); assert data["hermes_release"]==canonical and data["source_commit"]==commit and data["source_repository"]==source_repo; expected={"chat_completions/terminal_length.sse":"tag-source-derived","chat_completions/terminal_agent_error.sse":"tag-source-derived","chat_completions/terminal_task_exception_contradiction.sse":"tag-source-derived","chat_completions/terminal_design_matrix.json":"design-derived"}; entries={entry["path"]:entry for entry in data["fixtures"] if entry["path"] in expected}; assert set(entries)==set(expected) and all(entries[path]["evidence_kind"]==kind for path,kind in expected.items()); tag_entries=[entries[path] for path,kind in expected.items() if kind=="tag-source-derived"]; design=entries["chat_completions/terminal_design_matrix.json"]; assert all(entry["hermes_release"]==canonical and entry["source_commit"]==commit and isinstance(entry.get("source_refs"),list) and len(entry["source_refs"])&gt;0 and all(ref.startswith(source_repo+"/blob/") and commit in ref for ref in entry["source_refs"]) for entry in tag_entries); assert design["hermes_release"]==canonical and design["source_commit"]==commit and set(design.get("decision_refs",[]))=={"D-01","D-02","D-03","D-04"}; assert all(isinstance(entry.get("reproduction"),dict) and isinstance(entry["reproduction"].get("mode"),str) and bool(entry["reproduction"]["mode"]) and isinstance(entry["reproduction"].get("procedure"),list) and len(entry["reproduction"]["procedure"])&gt;0 and entry["reproduction"].get("configuration",{}).get("live_server_invoked") is False and isinstance(entry.get("semantic_assertions"),list) and len(entry["semantic_assertions"])&gt;0 and (root/entry["path"]).is_file() and hashlib.sha256((root/entry["path"]).read_bytes()).hexdigest()==entry["sha256"] for entry in entries.values())'</automated>
  </verify>
  <done>The terminal fixture corpus truthfully separates upstream-tag evidence from approved design-derived contract rows and rejects the known tagged contradiction by expectation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Official Hermes tag to repository fixture | Upstream source-derived facts become local immutable evidence. |
| Approved design to repository fixture | Contract-only rows must remain distinguishable from upstream behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-E1 | Spoofing | fixture provenance | high | mitigate | Record exact tag, peeled commit, immutable URLs, evidence kind, procedure, semantic assertions, and `live_server_invoked: false`. |
| T-02-E2 | Tampering | immutable fixture bytes | high | mitigate | Compute and verify SHA-256 for every manifest entry before implementation consumes it. |
| T-02-E3 | Tampering | latest-tag compatibility gate | high | mitigate | Re-enumerate numeric tags and stop under D-16 on any shape that cannot preserve identical semantics. |
| T-02-SC | Tampering | dependency metadata | low | accept | No package install or metadata change occurs; exact drift remains recorded for Phase 4 revalidation. |
</threat_model>

## Artifacts this phase produces

New public symbols: none.

New private symbols: none.

New fixture paths:
- `tests/fixtures/hermes/v2026.7.7.2/chat_completions/tool_progress_pair.sse`
- `tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_length.sse`
- `tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_agent_error.sse`
- `tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_task_exception_contradiction.sse`
- `tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_design_matrix.json`

<verification>
- Both task-level hash/provenance commands pass in under 60 seconds.
- `git diff -- pyproject.toml uv.lock` is empty after this plan.
- The executor summary records the observed latest numeric Hermes tag and the Phase 4 dependency currency constraint.
</verification>

<success_criteria>
- Production implementation can consume a complete, immutable, truthfully sourced lifecycle corpus.
- A newer or incompatible tag cannot silently broaden the canonical envelope.
- Evidence origin and hashes are machine-checkable for every new fixture.
</success_criteria>

<output>
Create `.planning/phases/02-conversation-event-contract/02-02-SUMMARY.md` when done.
</output>
