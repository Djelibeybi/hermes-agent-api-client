---
phase: 02-conversation-event-contract
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check_phase2_provenance.py
  - tests/fixtures/hermes/{observed-latest-tag}/provenance.json
  - tests/fixtures/hermes/{observed-latest-tag}/chat_completions/tool_progress_pair.sse
  - tests/fixtures/hermes/{observed-latest-tag}/chat_completions/terminal_length.sse
  - tests/fixtures/hermes/{observed-latest-tag}/chat_completions/terminal_agent_error.sse
  - tests/fixtures/hermes/v2026.7.7.2/chat_completions/tool_progress_pair.sse
  - tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_length.sse
  - tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_agent_error.sse
  - tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_task_exception_contradiction.sse
  - tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_design_matrix.json
  - tests/fixtures/hermes/v2026.7.7.2/provenance.json
autonomous: true
requirements:
  - TOOL-02
  - TOOL-03
  - TOOL-04
  - TERM-02
  - TERM-03
  - TERM-04
  - TERM-05
  - TERM-06
must_haves:
  truths:
    - "Every accepted or rejected lifecycle fixture has immutable source identity, an honest evidence kind, semantic assertions, and a matching SHA-256."
    - "Canonical v2026.7.7.2 and the latest numeric Hermes tag are verified before implementation proceeds."
    - "Tag-source-derived combinations and design-derived contract rows are never represented as live captures."
  artifacts:
    - path: "scripts/check_phase2_provenance.py"
      provides: "Reviewable release, compatibility, provenance, per-row citation, and SHA-256 gate"
      contains: "latest-tag-verification-blocked"
    - path: "tests/fixtures/hermes/v2026.7.7.2/chat_completions/tool_progress_pair.sse"
      provides: "Correlated running/completed lifecycle evidence"
    - path: "tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_design_matrix.json"
      provides: "Approved design-derived omission and unknown-code rows"
    - path: "tests/fixtures/hermes/v2026.7.7.2/provenance.json"
      provides: "Evidence kind, release, commit, source, semantic assertions, reproduction, and hashes"
    - path: "tests/fixtures/hermes/{observed-latest-tag}/provenance.json"
      provides: "Conditional D-16 ownership for compatible newer-tag evidence and structured difference summary"
  key_links:
    - from: "tests/fixtures/hermes/v2026.7.7.2/provenance.json"
      to: "tests/fixtures/hermes/v2026.7.7.2/chat_completions/"
      via: "one manifest entry and exact SHA-256 per immutable fixture"
      pattern: "(tag-source-derived|design-derived)"
    - from: "tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_design_matrix.json"
      to: ".planning/phases/02-conversation-event-contract/02-CONTEXT.md"
      via: "case-level decision citations for D-01 through D-04"
      pattern: "D-0[1-4]"
    - from: "scripts/check_phase2_provenance.py"
      to: "tests/fixtures/hermes/"
      via: "single committed verifier for canonical and conditional newer-tag evidence roots"
      pattern: "difference_summary"
---

<objective>
Freeze the versioned evidence corpus before production mapping code is written.

Purpose: Make D-01 through D-16 auditable at the exact upstream/version boundary, including truthful derivation under D-13 and D-14, the implementation-time latest-tag gate in D-15, and the block-or-version-specific rule in D-16.
Output: One committed provenance verifier, one correlated tool fixture, three tag-source-derived terminal fixtures, one design-derived terminal matrix, explicit latest-tag dispositions, and complete immutable provenance.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-conversation-event-contract/02-CONTEXT.md
@.planning/phases/02-conversation-event-contract/02-RESEARCH.md
@.planning/phases/02-conversation-event-contract/02-PATTERNS.md
@.planning/phases/02-conversation-event-contract/02-VALIDATION.md
@docs/superpowers/specs/2026-07-17-conversation-contract-design.md
@tests/fixtures/hermes/v2026.7.7.2/chat_completions/complete.sse
@tests/fixtures/hermes/v2026.7.7.2/provenance.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the immutable release target and freeze correlated tool evidence</name>
  <files>scripts/check_phase2_provenance.py, tests/fixtures/hermes/v2026.7.7.2/chat_completions/tool_progress_pair.sse, tests/fixtures/hermes/v2026.7.7.2/provenance.json, tests/fixtures/hermes/{observed-latest-tag}/provenance.json and chat_completions/tool_progress_pair.sse (conditional when D-16 activates)</files>
  <read_first>
    - tests/fixtures/hermes/v2026.7.7.2/chat_completions/tool_progress_pair.sse (create; verify absent before writing)
    - tests/fixtures/hermes/v2026.7.7.2/provenance.json
    - tests/fixtures/hermes/v2026.7.7.2/chat_completions/complete.sse
    - scripts/check_phase2_provenance.py (create; verify absent before writing)
    - .planning/phases/02-conversation-event-contract/02-CONTEXT.md
    - .planning/phases/02-conversation-event-contract/02-RESEARCH.md
    - .planning/phases/02-conversation-event-contract/02-PATTERNS.md
  </read_first>
  <action>Create `scripts/check_phase2_provenance.py` before fixture edits and make it the only executable gate for this plan; this directly addresses the review concern about escaped, unreviewable inline Python. The script must enumerate official numeric release tags, independently verify that canonical annotated tag `v2026.7.7.2` peels to `9de9c25f620ff7f1ce0fd5457d596052d5159596`, validate every declared fixture path/hash/evidence kind/reproduction/source reference, and exit distinctly for release blocking conditions. Source references must name an allowed tagged handler/test path at the exact peeled commit, include an exact line anchor, and be checked against the detached tagged tree; do not accept an arbitrary or nonexistent blob path merely because the URL contains the commit. If `git ls-remote` fails for any network/DNS/auth reason, record/report the exact disposition `latest-tag-verification-blocked` and stop; do not silently continue from pinned canonical evidence. Record the dynamically observed latest numeric tag and peeled commit in `release_verification`. If it is canonical, record `compatibility_disposition: canonical-current`. If a newer tag exists, inspect its exact tagged handler and tests and apply D-15/D-16: declare its conditional fixture root under `tests/fixtures/hermes/{observed-latest-tag}/`, store immutable captured evidence and hashes there, and require `compatibility_disposition: identical-public-semantics`, release/commit, commit-pinned source references, `evidence_kind: immutable-tag-capture`, `public_semantics: identical`, non-empty reproduction and semantic assertions, and `live_server_invoked: false`. Do not accept the word `identical` as self-authentication: require a non-empty structured `difference_summary` naming canonical and newer paths/shapes, each concrete change, and the public event produced; the script must compare the normalized public-event assertion for every newer fixture with its canonical counterpart. If the shape is incompatible, ambiguous, lacks immutable evidence, or cannot prove identical public semantics, stop before production code and surface a blocking `checkpoint:decision` to the user; add no generic alias or autonomous guess. Record the same disposition, comparison, and evidence paths in the plan summary for Plan 02-04. Then create a complete immutable SSE sequence containing tag-shaped `running` followed by `completed` for the same `toolCallId`, including additive emoji/label only where tagged source emits them, followed by valid stop and DONE. Add a `tag-source-derived` manifest entry with exact canonical release/commit, commit-pinned `source_refs`, reproduction, `live_server_invoked: false`, ordered-correlation semantic assertions, and SHA-256. Keep the historical top-level `evidence_scope.live_server_tested` field unchanged: for new entries, `reproduction.configuration.live_server_invoked` is authoritative; schema normalization is explicitly deferred to Phase 4. Do not call this source-derived fixture a capture, invent arguments/results, or change dependencies.</action>
  <acceptance_criteria>
    - The canonical tag independently peels to the pinned commit, and provenance records the dynamically observed latest numeric tag plus peeled commit.
    - A canonical-current latest tag is accepted directly; a newer tag is accepted only with immutable version-specific captured evidence and an `identical-public-semantics` disposition, while incompatible or ambiguous shapes stop execution under D-16.
    - Network failure produces exactly `latest-tag-verification-blocked`; pinned evidence alone cannot complete D-15.
    - A compatible newer tag owns a declared version-specific fixture root and includes a concrete structured difference summary whose normalized public events match canonical evidence; ambiguity triggers a blocking human contract decision.
    - The fixture preserves the exact correlated ID and running/completed order required by TOOL-02.
    - The manifest truthfully classifies derivation and machine-verifies canonical release/source identities, reproduction metadata, `live_server_invoked: false`, semantic assertions, and matching SHA-256 bytes.
    - Commit-pinned source references resolve to allowed files and exact line anchors in the detached tagged tree; nonexistent or arbitrary blob paths fail the gate.
  </acceptance_criteria>
  <verify>
    <automated>uv run --no-sync python scripts/check_phase2_provenance.py --scope release-and-tool</automated>
  </verify>
  <done>Canonical and latest release identity is proven, and the immutable correlated tool fixture is hash-linked to honest tagged-source provenance.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze terminal acceptance, unknown-code, and contradiction evidence</name>
  <files>scripts/check_phase2_provenance.py, tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_length.sse, tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_agent_error.sse, tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_task_exception_contradiction.sse, tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_design_matrix.json, tests/fixtures/hermes/v2026.7.7.2/provenance.json</files>
  <read_first>
    - tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_length.sse (create; verify absent before writing)
    - tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_agent_error.sse (create; verify absent before writing)
    - tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_task_exception_contradiction.sse (create; verify absent before writing)
    - tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_design_matrix.json (create; verify absent before writing)
    - tests/fixtures/hermes/v2026.7.7.2/provenance.json
    - .planning/phases/02-conversation-event-contract/02-CONTEXT.md
    - .planning/phases/02-conversation-event-contract/02-RESEARCH.md
    - docs/superpowers/specs/2026-07-17-conversation-contract-design.md
    - scripts/check_phase2_provenance.py
  </read_first>
  <action>Create complete tag-source-derived SSE fixtures for the canonical length row, the canonical valid agent-error row with exact server partial, and the tagged task-exception contradiction with `completed=true` plus `failed=true`. Create `terminal_design_matrix.json` for the explicitly approved but non-emitted omission cases and bounded unknown-code case; every individual matrix row must state expected accept/reject semantics and include one or more applicable citations from D-01 through D-04, not rely only on top-level `decision_refs`. Keep lifecycle metadata only in root `hermes` per D-09/D-10, include ignored raw error canaries only to prove D-11, and represent duplicate-member cases later as raw bytes because ordinary JSON objects cannot preserve them under D-12. Extend the committed verifier so it checks the explicit path-to-kind mapping, each row's decision citations, exact source identities and immutable references, non-empty reproduction, authoritative per-entry `live_server_invoked: false`, semantic assertions, and exact SHA-256. The three SSE paths are `tag-source-derived`; the matrix is `design-derived`. Do not label source-derived or design-derived bytes as captured. Record in the plan summary that the historical `live_server_tested` and new per-entry `live_server_invoked` names intentionally coexist until Phase 4, and that Ruff 0.15.22, prek 0.4.10, coverage 7.15.2, and GitPython 3.1.52 are active Phase 4 currency constraints. Make no Phase 2 pin/lock edit.</action>
  <acceptance_criteria>
    - Valid length and agent-error rows, the tagged contradiction, approved omissions, and the unknown bounded code each have immutable evidence.
    - D-01 through D-04 and D-09 through D-14 are traceable from fixture rows and provenance without overstating origin.
    - The verifier rejects any terminal matrix row without its own applicable D-01 through D-04 citation.
    - Every new fixture hash matches and no dependency file changes.
  </acceptance_criteria>
  <verify>
    <automated>uv run --no-sync python scripts/check_phase2_provenance.py --scope terminal</automated>
  </verify>
  <done>The terminal fixture corpus truthfully separates upstream-tag evidence from approved design-derived contract rows and rejects the known tagged contradiction by expectation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Official Hermes tag to repository fixture | Upstream source-derived facts become local immutable evidence. |
| Approved design to repository fixture | Contract-only rows must remain distinguishable from upstream behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-E1 | Spoofing | fixture provenance | high | mitigate | Record exact tag, peeled commit, immutable URLs, evidence kind, procedure, semantic assertions, and `live_server_invoked: false`. |
| T-02-E2 | Tampering | immutable fixture bytes | high | mitigate | Compute and verify SHA-256 for every manifest entry before implementation consumes it. |
| T-02-E3 | Tampering | latest-tag compatibility gate | high | mitigate | Re-enumerate numeric tags, use exact `latest-tag-verification-blocked` on network failure, require structured difference evidence for identical semantics, and surface a blocking human contract decision for ambiguity. |
| T-02-SC | Tampering | dependency metadata | low | accept | No package install or metadata change occurs; exact drift remains recorded for Phase 4 revalidation. |
</threat_model>

## Artifacts this phase produces

New public symbols: none.

New private symbols: none.
New verification symbols:
- `scripts/check_phase2_provenance.py` with `release-and-tool` and `terminal` scopes

New fixture paths:
- `tests/fixtures/hermes/v2026.7.7.2/chat_completions/tool_progress_pair.sse`
- `tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_length.sse`
- `tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_agent_error.sse`
- `tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_task_exception_contradiction.sse`
- `tests/fixtures/hermes/v2026.7.7.2/chat_completions/terminal_design_matrix.json`
- `tests/fixtures/hermes/{observed-latest-tag}/provenance.json`
- `tests/fixtures/hermes/{observed-latest-tag}/chat_completions/tool_progress_pair.sse`
- `tests/fixtures/hermes/{observed-latest-tag}/chat_completions/terminal_length.sse`
- `tests/fixtures/hermes/{observed-latest-tag}/chat_completions/terminal_agent_error.sse`

The four `{observed-latest-tag}` paths are conditional ownership only: create them when D-16 activates with proven identical public semantics; otherwise they remain absent.

<verification>
- Both task-level hash/provenance commands pass in under 60 seconds.
- `git diff -- pyproject.toml uv.lock` is empty after this plan.
- The executor summary records the observed latest numeric Hermes tag and the Phase 4 dependency currency constraint.
</verification>

<success_criteria>
- Production implementation can consume a complete, immutable, truthfully sourced lifecycle corpus.
- A newer or incompatible tag cannot silently broaden the canonical envelope.
- Evidence origin and hashes are machine-checkable for every new fixture.
</success_criteria>

<output>
Create `.planning/phases/02-conversation-event-contract/02-02-SUMMARY.md` when done.
</output>
